# AGENT.md

이 문서는 현재 프로젝트를 완성하기 위한 에이전트 작업 지침이다.

`README2.md`의 초기 기획에서 프로젝트 정체성이 변경되었다. 구현 중 `README2.md`와 이 문서가 충돌하면 이 `AGENT.md`를 우선한다.

## 프로젝트 정체성

- 서비스 대상: 부산을 방문하는 일반 여행객
- 서비스 형태: 웹사이트 + 크롬 확장 프로그램
- 핵심 데이터: 접근 가능한 온라인 예약 업체의 예약/약관/계약 관련 데이터
- 목적: 부산 여행객이 온라인 예약 과정에서 놓치기 쉬운 취소, 환불, 책임, 이용 조건, 주의사항을 AI가 쉽게 풀어 카드로 보여주고, 원문 근거와 함께 확인할 수 있게 돕는다.
- 핵심 데모: 사용자가 온라인 예약 페이지에서 크롬 확장 프로그램을 실행하면, 확장 프로그램이 페이지의 계약/약관/예약 조건 텍스트를 추출하고, Chrome Side Panel 안에서 바로 AI 주의사항 카드를 보여준다. 웹사이트는 같은 분석 경험의 상세 보기, 샘플 실행, 기록 관리 화면으로 사용한다.

## 변경된 전제

- 사진 촬영 기능은 사용하지 않는다.
- 종이 서약서 OCR 또는 촬영본 문서 파싱은 우선순위에서 제외한다.
- 해양 레저 업체 전용 서비스가 아니다.
- 부산 여행 전반의 온라인 예약 환경을 대상으로 한다.
- 단순 정적 웹사이트가 아니라 실제 사용 흐름이 있는 웹 앱과 크롬 확장 프로그램을 함께 만든다.
- 접근 가능한 계약 데이터가 온라인 예약 업체 하나뿐이라는 현실을 설계 기준으로 삼는다.

## 핵심 철학

1. AI는 판단하지 않고 이해를 돕는다.
   - 약관이나 예약 조건이 유리하다/불리하다, 위법하다/합법이다처럼 단정하지 않는다.
   - 사용자가 확인해야 할 내용을 쉬운 말로 바꿔준다.

2. 원문 근거가 항상 있어야 한다.
   - AI 카드에는 반드시 `source`를 포함한다.
   - 원문에 없는 내용을 추가하지 않는다.
   - 사용자가 AI 설명과 원문을 바로 대조할 수 있어야 한다.

3. 온라인 예약의 불편함을 줄인다.
   - 긴 약관, 작은 글씨, 흩어진 안내 문구를 사용자가 읽기 쉬운 카드로 바꾼다.
   - 중요한 조건을 놓치지 않게 돕되, 사용자의 선택을 대신하지 않는다.

4. 부산 여행객 맥락을 유지한다.
   - 숙박, 체험, 교통, 투어, 액티비티 등 부산 여행 예약 상황에 맞는 문안과 UX를 우선한다.
   - 지역성과 여행자 보호 관점을 살리되, 특정 업종에 과하게 묶이지 않는다.

## 제품 구성

### 1. 웹사이트

웹사이트는 확장 프로그램과 같은 분석 경험을 더 넓은 화면에서 제공하고, 샘플 실행과 기록 관리를 맡는 주 화면이다.

- AI 요약 결과 표시
- 위험도/중요도 카드 리스트 표시
- 원문 보기 및 출처 확인
- 전체 확인 체크
- 예약 조건 확인 완료 화면
- 크롬 확장 프로그램 설치/연결 안내
- 데모용 샘플 예약 데이터 실행
- 확장 프로그램에서 "웹에서 자세히 보기"로 넘어온 분석 결과 표시

### 2. 크롬 확장 프로그램

크롬 확장 프로그램은 온라인 예약 페이지에서 텍스트를 가져오고, 사용자가 페이지를 떠나지 않고 Chrome Side Panel에서 바로 주의사항을 확인하는 핵심 화면이다.

- Manifest V3 기준으로 만든다.
- content script로 현재 페이지의 예약/약관/취소/환불/주의사항 텍스트를 추출한다.
- Side Panel UI에서 "이 예약 조건 분석하기" 흐름을 제공한다.
- 확장 프로그램 안에서 웹사이트와 같은 요약, 카드 리스트, 원문 보기, 확인 체크 UI를 제공한다.
- 추출한 데이터를 백엔드 분석 API 또는 mock 분석기로 전달하고, 결과를 확장 프로그램에 직접 표시한다.
- 필요할 때만 "웹에서 자세히 보기" 버튼으로 웹 앱 분석 화면을 연다.
- 확장 프로그램은 가능한 한 특정 업체 의존 코드를 분리해 둔다.
- 접근 가능한 온라인 예약 업체 하나를 우선 지원하고, 나중에 사이트 어댑터를 추가할 수 있게 설계한다.

## 개발 우선순위

1. 크롬 확장 프로그램의 직접 분석 흐름
   - 현재 탭에서 텍스트 추출
   - 예약 조건 후보 텍스트 필터링
   - AI 분석 요청 또는 mock 분석
   - 확장 프로그램 안에서 요약과 주의사항 카드 표시
   - 카드별 원문 확인 체크

2. 웹 앱의 분석 결과 화면
   - 요약
   - 카드 리스트
   - 중요도 표시
   - 원문 접기/펼치기
   - 사용자의 확인 체크
   - 확장 프로그램과 같은 컴포넌트/스키마 재사용

3. AI 분석 인터페이스
   - 추출 텍스트를 정해진 JSON 스키마로 변환
   - API가 없을 때도 데모 가능한 mock 응답 제공

4. 실제 온라인 예약 업체 1곳에 맞춘 데모 완성
   - 범용성보다 발표 가능한 완성 흐름을 우선한다.
   - 다만 업체별 추출 로직은 어댑터로 분리해 확장 가능성을 보여준다.

5. 저장/공유/증빙 기능
   - 필요 시 분석 결과 저장, 예약 URL 기록, 확인 시각 기록을 추가한다.

## 권장 기술 스택

- 웹 프론트엔드: React + Vite + Tailwind CSS
- 크롬 확장: Manifest V3, Side Panel API, content script, service worker
- AI 해설 생성: Upstage Solar LLM
- 데이터 추출: DOM 기반 텍스트 추출, 사이트별 selector adapter
- 백엔드/API 서버: FastAPI
- 백엔드/DB: 필요 시 Supabase 또는 간단한 저장소
- 상태 관리: 앱 상태를 우선 사용하고, 핵심 기록은 백엔드 저장을 고려한다.

## 개발 서버 운영 규칙

- 기본 로컬 확인 URL은 FastAPI가 제공하는 `http://127.0.0.1:8000/`이다.
- Vite dev server를 기본 라이브 서버로 쓰지 않는다. 프론트엔드는 빌드한 뒤 FastAPI가 `frontend/dist`를 정적 파일로 서빙한다.
- 프론트엔드 변경 사항을 FastAPI 화면에 반영하려면 아래 명령을 실행한다.

```powershell
cd frontend
npm.cmd run build
```

- FastAPI 서버는 프로젝트 가상환경 Python으로 실행한다.

```powershell
cd backend
..\.venv\Scripts\python.exe -m uvicorn main:app --host 127.0.0.1 --port 8000
```

- PowerShell에서 `npm`이 실행 정책 문제로 막히면 `npm.cmd`를 사용한다.
- 서버 실행에 `--reload`, 복잡한 로그 리다이렉트, 긴 `Start-Process` 명령을 기본으로 쓰지 않는다. 작업 흐름이 멈추지 않도록 단순 실행을 우선한다.
- 백그라운드 실행이 꼭 필요할 때만 짧은 `Start-Process`를 사용하고, 실행 후 `http://127.0.0.1:8000/api/health`로 서버 상태를 확인한다.
- FastAPI 확인 URL:
  - 웹 앱: `http://127.0.0.1:8000/`
  - 헬스 체크: `http://127.0.0.1:8000/api/health`
  - API 문서: `http://127.0.0.1:8000/docs`

## 데이터 흐름

```text
온라인 예약 페이지
  -> 크롬 확장 content script
  -> 예약/약관/취소/환불/주의사항 텍스트 추출
  -> side panel에서 분석 요청
  -> 확장 프로그램 안에서 AI 카드 표시
  -> 사용자가 카드별 원문 확인
  -> 필요 시 웹 앱 상세 화면 열기
  -> 확인 완료 결과 저장 또는 안내
```

## AI 카드 JSON 스키마

LLM 응답은 반드시 아래 JSON 구조로만 사용한다. 백틱, 인사말, 부가 설명이 섞이면 정제하거나 실패 처리한다.

```json
{
  "summary": "예약 조건 전체가 무엇에 관한 것인지 1~2문장 요약",
  "cards": [
    {
      "title": "항목 제목",
      "level": "high | medium | low",
      "plain": "쉬운 말로 풀어쓴 설명",
      "question": "예약 전에 확인하면 좋을 질문 한 문장",
      "source": "이 카드가 근거한 원문 문장 발췌"
    }
  ]
}
```

## 중요도 등급 기준

- `high`: 환불 불가, 취소 수수료, 노쇼, 추가 결제, 사고/분실 책임, 이용 제한처럼 예약 결정에 큰 영향을 줄 수 있는 항목
- `medium`: 체크인/이용 시간, 변경 조건, 준비물, 보험/보증금, 현장 결제처럼 확인이 필요한 항목
- `low`: 일반 안내, 위치, 문의 방법, 기본 준비 안내처럼 참고용 항목

등급은 사용자의 주의를 돕기 위한 표시이며 법적 판단처럼 표현하지 않는다.

## 구현 규칙

- 사진 업로드, 카메라 캡처, OCR 중심 기능을 새로 만들지 않는다.
- 온라인 페이지에서 가져온 텍스트를 주요 입력으로 삼는다.
- 사이트별 DOM 추출 로직은 `adapter` 개념으로 분리한다.
- AI 응답은 반드시 JSON 파싱 후 사용한다.
- JSON 파싱 실패, 빈 카드, 잘못된 level 값에 대한 에러 처리를 둔다.
- 카드 수는 기본적으로 최대 6개를 권장한다.
- 각 카드에는 원문 발췌 `source`가 있어야 한다.
- 확장 프로그램 권한은 필요한 범위로만 요청한다.
- API 키와 민감 정보는 프론트엔드나 확장 프로그램 코드에 하드코딩하지 않는다.
- 실제 API 연동이 불확실한 경우 mock 데이터와 실제 API 경계를 명확히 분리한다.

## 크롬 확장 구현 규칙

- Manifest V3를 사용한다.
- `content_scripts`는 페이지 텍스트 추출에 집중한다.
- `sidePanel`은 사용자의 명령, 분석 로딩, 요약, 카드 리스트, 원문 보기, 확인 체크를 담당한다.
- `action` 버튼은 Side Panel을 여는 진입점으로 사용한다.
- `background` 또는 service worker는 탭 정보, 메시지 전달, 분석 요청, 웹 앱 상세 화면 열기를 담당한다.
- 온라인 예약 업체 1곳의 selector는 별도 파일로 분리한다.
- 범용 fallback 추출은 제목, 본문, 버튼 주변 텍스트, 약관/취소/환불 키워드가 있는 영역을 우선한다.
- 사용자 개인정보, 결제정보, 주민등록번호, 카드번호 등 민감정보는 수집하지 않는다.
- 분석 결과 UI는 웹 앱과 최대한 같은 컴포넌트 구조, 타입, JSON 스키마를 공유한다.
- Side Panel은 좁은 보조 화면이므로 핵심 카드와 원문 발췌를 먼저 보여주고, 긴 내용은 접기/펼치기 또는 웹 앱 상세 화면으로 넘긴다.

## 웹 앱 UX 규칙

- 첫 화면은 실제 분석 경험으로 바로 진입할 수 있어야 한다.
- 단순 홍보용 랜딩 페이지를 만들지 않는다.
- 데모용 샘플 데이터 분석 버튼을 제공해 확장 프로그램 없이도 흐름을 확인할 수 있게 한다.
- 카드 UI는 빠르게 훑어볼 수 있게 구성한다.
- 원문 보기 기능을 통해 사용자가 AI 설명의 근거를 확인할 수 있게 한다.
- 웹 앱은 확장 프로그램에서 본 결과를 더 넓게 확인하는 상세 화면 역할도 한다.
- 문안은 부산 여행객이 예약 전에 확인하는 상황을 기준으로 쓴다.
- 불안을 과하게 키우는 표현보다 "확인해보세요", "예약 전에 물어보면 좋아요" 같은 표현을 우선한다.

## 목표 과제

- 실제 접근 가능한 온라인 예약 업체의 DOM 구조와 데이터 위치를 확인해야 한다.
- 크롬 확장 프로그램이 해당 사이트에서 필요한 권한만으로 텍스트를 안정적으로 추출할 수 있는지 검증해야 한다.
- AI 분석 API를 실제로 사용해 유의미한 데이터를 취득해야 한다.
- 분석 결과 저장이 필요없기 때문에 단순한 API 요청으로 처리한다.
- 모두싸인 연동을 유지한다. 모두 사인개발을 앞서 말한 AI 요약 및 사용자에게 위험도가 있는 것들을 요약/정리 기능을 만든 이후 개발을 시작한다.

## 작업 방식

- 새 기능을 만들기 전에 현재 코드 구조와 기존 패턴을 먼저 확인한다.
- 웹 앱과 확장 프로그램을 별도 산출물로 보되, 분석 결과 UI, 타입, 스키마는 가능한 한 공유한다.
- 데모 가능한 전체 흐름을 우선하고, 불확실한 외부 API는 mock/fallback으로 막힘 없이 이어지게 만든다.
- 구현 중 이전 기획의 촬영, 해양 레저 전용, 종이 서약서 OCR 전제가 나오면 제거하거나 새 정체성에 맞게 바꾼다.
- 사용자가 접근 가능한 온라인 예약 업체 1곳을 기준으로 먼저 완성한 뒤, 확장 가능성은 어댑터 구조로 보여준다.
