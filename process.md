# process.md

이 문서는 현재 코드베이스와 `AGENT.md`가 목표로 하는 결과물 사이의 차이를 정리하고, 프로젝트를 완성하기 위한 실행 순서를 정의한다.

## 0. 현재 코드베이스 상태

### 프론트엔드

- 위치: `frontend/`
- 기술: React + Vite
- 현재 화면은 `Home` 중심이며, 샘플 예약 원문과 Swiper 카드 분석 결과를 바로 보여준다.
- 첫 화면 문안은 "부산 여행 예약 조건 분석 서비스"로 정리되었다.
- 파일 업로드/OCR 중심 `UploadForm`은 제거되었다.
- `Result` 페이지도 샘플 예약 원문과 분석 결과를 렌더링한다.
- 공통 분석 스키마, mock 분석 결과, Swiper cards UI가 추가되었다.
- Tailwind CSS는 목표 스택에 있지만 현재 `package.json`에는 설치되어 있지 않다.
- 크롬 확장 프로그램 기본 구조가 `extension/`에 추가되었다.

### 백엔드

- 위치: `backend/main.py`
- FastAPI 앱이 `frontend/dist`를 정적 파일로 서빙한다.
- `/api/health`가 있다.
- `/contractsParsing`, `/contractsParsingOCR`는 현재 제품 방향에서 제외된 legacy 엔드포인트로 410을 반환한다.
- `/api/analyze-reservation`은 Upstage Solar LLM을 호출해 예약 조건 분석 JSON을 반환한다.
- `/simpleAIResponse`는 Upstage Solar LLM을 단순 호출하는 테스트 엔드포인트다.
- JSON 스키마 강제 출력, 파싱 실패 처리, 빈 카드 처리, level 검증이 있다.
- 긴 예약 원문은 백엔드에서 chunk 단위로 1차 분석한 뒤 최종 카드 6개로 병합한다.
- 모두싸인 연동은 아직 구현되어 있지 않다.

### 문서/기획 상태

- `AGENT.md`는 Chrome Side Panel 중심의 새 정체성으로 정리되었다.
- 실제 개발 방향은 사용자의 최근 결정에 따라 Chrome Side Panel 우선으로 잡는다.
- 모두싸인 연동은 유지하되, AI 요약/주의사항 정리 기능 이후 단계로 둔다.

## 1. 목표 결과물

### 핵심 데모

부산 여행객이 온라인 예약 페이지에서 크롬 확장 프로그램을 열면, Chrome Side Panel이 현재 페이지의 예약/약관/취소/환불/주의사항 텍스트를 추출하고 AI가 정리한 주의사항 카드를 바로 보여준다.

### 구성

- 웹 앱: 분석 결과 상세 보기, 샘플 데이터 실행, 확장 프로그램 안내
- 크롬 확장 프로그램: Side Panel 기반 즉석 분석 화면
- 백엔드 API: 추출 텍스트를 받아 AI 카드 JSON으로 변환
- 공통 스키마: 웹 앱과 확장 프로그램이 같은 분석 결과 구조 사용

### 우선순위

1. 온라인 예약 텍스트 추출
2. Side Panel에서 바로 분석 결과 표시
3. 웹 앱에서 같은 결과 상세 표시
4. 실제 AI 분석 API 연결
5. 모두싸인 연동

## 2. 핵심 차이

| 영역 | 현재 상태 | 목표 상태 | 해야 할 일 |
|---|---|---|---|
| 제품 정체성 | 해양 레저 계약/서약서 중심 | 부산 일반 여행객 온라인 예약 조건 중심 | 문안, 화면, 데이터 모델 교체 |
| 입력 방식 | 파일 업로드/OCR 중심 | 예약 페이지 DOM 텍스트 추출 중심 | 업로드 폼 제거 또는 데모 보조로 격하 |
| 크롬 확장 | 없음 | Manifest V3 + Side Panel | `extension/` 신규 구축 |
| 분석 화면 | placeholder | 요약, 카드, 원문, 확인 체크 | 공통 결과 UI 구현 |
| AI API | 단순 프롬프트 테스트 | 예약 조건 전용 JSON 카드 생성 | `/analyze-reservation` 구현 |
| 민감정보 | API Key를 화면에서 입력 | 서버 환경변수 사용 | `.env` 기반으로 변경 |
| 모두싸인 | 미구현 | AI 분석 이후 연동 | 후순위 작업으로 분리 |

## 3. 구현 프로세스

### Phase 1. 기획/문서 정렬

상태: 완료

- `AGENT.md`에서 남아 있는 `popup` 중심 표현을 Side Panel 중심으로 정리한다.
- README류 문서가 이전 기획을 강하게 가리키면, 새 정체성 기준으로 보조 설명을 추가한다.
- 프로젝트 이름과 화면 문안을 "부산 여행객 온라인 예약 조건 분석" 방향으로 통일한다.

완료 기준:

- 사진, 촬영, OCR, 해양 레저 전용 표현이 핵심 플로우에서 제거된다.
- 크롬 확장 기본 UI가 Side Panel임이 문서에 명확하다.

수행 기록:

- `AGENT.md`의 핵심 데모, 제품 구성, 개발 우선순위, 기술 스택, 데이터 흐름, 크롬 확장 구현 규칙을 Chrome Side Panel 기준으로 정리했다.
- `README.md`에 현재 프로젝트 방향을 추가했다.
- `README2.md` 상단에 초기 기획 문서이며 최신 구현 기준은 `AGENT.md`라는 안내를 추가했다.
- `frontend/src/pages/Home.jsx`의 첫 화면 문안을 해양 레저 계약 분석에서 부산 여행 예약 조건 분석으로 바꿨다.

### Phase 2. 공통 분석 스키마 만들기

상태: 완료

- `summary`, `cards`, `title`, `level`, `plain`, `question`, `source` 구조를 공통 타입으로 정의한다.
- `high | medium | low` 외의 level 값은 fallback 처리한다.
- 카드가 비어 있거나 source가 없을 때의 에러/경고 상태를 정한다.
- mock 분석 결과를 만든다.

완료 기준:

- 웹 앱과 확장 프로그램이 같은 분석 결과 JSON을 사용할 수 있다.
- 실제 AI API 없이도 데모 화면이 동작한다.

수행 기록:

- 분석 결과 스키마, level 정규화, fallback 처리 로직을 추가했다.
- 부산 숙소 예약 조건 mock 원문과 mock 분석 결과를 추가했다.
- `AnalysisResult`와 `ContractCard`를 실제 카드 UI로 재구성했다.
- Swiper `EffectCards`를 적용해 분석 카드를 카드 스택으로 보여준다.

### Phase 3. 웹 앱 재구성

상태: 완료

- `Home` 문안을 새 정체성에 맞게 바꾼다.
- 파일 업로드 중심 `UploadForm`을 제거하거나 샘플 분석/텍스트 입력 중심으로 바꾼다.
- `Result`를 실제 결과 화면으로 구현한다.
- `ContractCard`를 예약 조건 카드 컴포넌트로 재구성한다.
- 카드별 확인 체크와 전체 확인 완료 상태를 만든다.
- 원문 접기/펼치기를 구현한다.
- Tailwind CSS를 도입할지 결정하고, 도입한다면 설치와 설정을 완료한다.

완료 기준:

- 웹 앱에서 샘플 예약 데이터를 분석 결과처럼 볼 수 있다.
- 사용자는 카드별로 원문을 확인하고 체크할 수 있다.

수행 기록:

- `UploadForm`과 기존 `/contractsParsing` 호출용 `contractApi`를 제거했다.
- `Home`을 샘플 예약 원문과 분석 카드 중심 화면으로 재구성했다.
- `ReservationSourcePanel`을 추가해 원문을 화면 왼쪽에서 확인할 수 있게 했다.
- `Result` placeholder를 실제 샘플 분석 결과 화면으로 교체했다.
- Tailwind CSS는 이번 단계에서 도입하지 않고, 기존 plain CSS를 확장했다.

### Phase 4. 백엔드 분석 API 만들기

상태: 완료

- 새 엔드포인트: `POST /api/analyze-reservation`
- 입력: `url`, `pageText`, `siteName` 또는 `sourceMeta`
- 출력: AI 카드 JSON 스키마
- Upstage Solar LLM 프롬프트를 예약 조건 분석 목적에 맞게 작성한다.
- LLM 응답에서 JSON만 파싱하고, 실패 시 명확한 에러를 반환한다.
- API Key는 클라이언트 입력이 아니라 서버 환경변수로 읽는다.
- 기존 `/contractsParsing`, `/contractsParsingOCR`는 우선순위에서 제외하거나 legacy로 분리한다.

완료 기준:

- 온라인 예약 텍스트를 보내면 AI 카드 JSON이 반환된다.
- 잘못된 응답과 네트워크 실패에 대한 처리가 있다.

수행 기록:

- Upstage API 문서의 OpenAI 호환 방식에 맞춰 `https://api.upstage.ai/v1` base URL과 `solar-pro3` 모델 별칭을 사용하도록 구현했다.
- `response_format`의 `json_schema`와 `strict: true`를 사용해 `summary`, `cards`, `title`, `level`, `plain`, `question`, `source` 구조만 반환되게 했다.
- 모든 필드를 필수로 두고 추가 필드를 막는 strict JSON Schema를 작성했다.
- `UPSTAGE_API_KEY`를 서버 환경변수 또는 루트 `.env`에서 읽도록 바꿨다.
- LLM JSON 파싱 실패, 스키마 불일치, 빈 카드, 카드 6개 초과, Upstage 호출 실패를 명확한 API 에러로 처리한다.
- LLM 응답이 마크다운, 설명문, 코드블록을 섞어 반환해도 백엔드가 JSON 객체를 최대한 추출해 파싱한다.
- 그래도 파싱/스키마 검증에 실패하면 동일 LLM에 JSON 복구 요청을 한 번 더 보내고, 복구된 결과를 다시 검증한다.
- 최종 실패 시 백엔드가 LLM 응답 preview를 에러에 포함하고, Side Panel 실패 화면에서 접기/펼치기로 확인할 수 있게 한다.
- chunk 분석에서는 카드 수를 최대 3개로 제한하고, source를 1~2문장으로 짧게 유지하도록 검증/복구한다.
- LLM이 긴 source를 쓰다가 응답이 중간에 잘리는 문제를 줄이기 위해 chunk 응답 토큰을 늘리고, 긴 조항 전체 발췌를 금지한다.
- LLM 응답이 유효한 JSON이지만 summary/source 등 일부 필드가 긴 경우에는 실패 처리하지 않고 서버에서 안전한 길이로 정규화한 뒤 검증한다.
- `/contractsParsing`, `/contractsParsingOCR`는 410 legacy 응답으로 분리했다.
- 실제 Upstage 키가 없는 환경에서는 503으로 실패하는 것을 확인했다.

### Phase 5. 크롬 확장 프로그램 기본 구조 생성

상태: 완료

- `extension/` 디렉터리를 만든다.
- Manifest V3 설정을 작성한다.
- `side_panel` 화면을 만든다.
- `action` 버튼을 누르면 Side Panel이 열리게 한다.
- `content script`가 현재 탭의 텍스트를 추출하게 한다.
- `service worker`가 content script, side panel, 백엔드 API 사이 메시지를 중계한다.

권장 구조:

```text
extension/
  manifest.json
  src/
    background.js
    contentScript.js
    sidepanel/
      index.html
      sidepanel.js
      sidepanel.css
    adapters/
      genericAdapter.js
      targetSiteAdapter.js
```

완료 기준:

- 로컬 Chrome에서 압축해제 확장 프로그램으로 불러올 수 있다.
- Side Panel에서 현재 페이지 텍스트 추출 결과를 볼 수 있다.

수행 기록:

- `extension/manifest.json`을 Manifest V3와 Chrome Side Panel 기준으로 추가했다.
- `background.js`에서 action 버튼으로 Side Panel을 열고, 현재 탭 텍스트 추출 요청을 중계하도록 했다.
- `contentScript.js`에서 페이지 텍스트 추출 메시지를 받고 adapter를 실행하도록 했다.
- `genericAdapter.js`에서 현재 페이지의 읽을 수 있는 텍스트를 추출하도록 했다.
- `targetSiteAdapter.js`에 업체별 adapter 뼈대를 추가했다.
- 추출 텍스트에서 이메일, 전화번호, 긴 숫자열을 기본 마스킹하도록 했다.
- `sidepanel/index.html`, `sidepanel.js`, `sidepanel.css`로 현재 페이지 텍스트 추출 결과를 표시하는 기본 UI를 만들었다.
- `extension/README.md`에 로컬 Chrome 압축해제 로드 방법을 정리했다.
- manifest JSON 파싱과 extension JS 문법 검사를 통과했다.

### Phase 6. 예약 업체 1곳 어댑터 구현

상태: 완료

- 접근 가능한 온라인 예약 업체의 DOM 구조를 확인한다.
- 어댑터의 목적은 약관을 해석하거나 중요 내용을 임의로 선별하는 것이 아니라, 현재 페이지의 읽을 수 있는 원문을 가능한 한 온전한 순서로 수집하는 것이다.
- 예약명, 일정, 취소/환불, 주의사항, 약관 영역은 누락 여부를 확인하기 위한 체크포인트로만 사용한다.
- 기본은 페이지 전체 텍스트 추출이며, nav/footer/광고/중복 UI처럼 계약 의미를 거의 담지 않는 반복 영역만 줄인다.
- 조항, 예외, 책임 범위, 별도 공급업체 약관 안내처럼 앞뒤 맥락이 필요한 문장은 잘라내지 않는다.
- 업체 전용 selector를 `targetSiteAdapter`로 분리한다.
- selector가 실패하면 generic adapter로 fallback한다.
- 개인정보, 결제정보, 이메일, 전화번호, 긴 숫자열은 추출 후 마스킹한다.
- 텍스트가 너무 길어 LLM 입력 한계를 넘는 경우에만 섹션 단위 chunking 또는 1차 압축 전략을 적용한다.
- 중요도 판단과 카드 선별은 adapter가 아니라 백엔드 AI 프롬프트와 JSON 검증 단계에서 수행한다.

완료 기준:

- 실제 예약 업체 페이지에서 약관/예약 조건 원문이 의미를 해치지 않는 순서로 안정적으로 추출된다.
- 업체별 로직과 범용 로직이 분리되어 있다.
- 어댑터가 계약 내용을 임의로 요약, 삭제, 중요도 판단하지 않는다.
- 민감정보 후보가 백엔드 전송 전에 마스킹된다.

수행 기록:

- 타깃 업체를 Airbnb로 정했다.
- `targetSiteAdapter`를 Airbnb 도메인 감지 방식으로 전환했다.
- Airbnb listing, checkout, policy 계열 URL을 `pageKind`로 구분하는 기본 분류를 추가했다.
- Airbnb 어댑터는 중요 영역을 임의로 고르지 않고, 읽을 수 있는 페이지 원문을 순서대로 추출한다.
- `genericAdapter`도 키워드 후보 선별 방식에서 전체 원문 보존 방식으로 조정했다.
- script, style, 이미지, 영상, iframe 등 비텍스트 노드만 제외하고 약관 본문 자체는 자르지 않도록 했다.
- 이메일, 전화번호, 긴 숫자열은 백엔드 전송 전에 마스킹한다.
- 추출 텍스트가 50,000자를 넘으면 `requiresChunking` 상태를 함께 반환한다.

### Phase 6-1. 긴 약관 입력 처리 전략

상태: 완료

- 일반적인 약관 페이지는 전체 원문을 그대로 AI API에 보낸다.
- LLM 입력 한계를 넘는 긴 문서는 원문 순서를 보존한 chunk로 나눈다.
- chunking이 필요한 경우에도 취소/환불/책임 같은 키워드만 잘라 보내지 않는다.
- 각 chunk 분석 결과를 합쳐 최종 카드 6개 이내로 다시 정리하는 2단계 분석을 검토한다.
- chunk별 원문 위치 또는 섹션 제목을 보존해 source 근거가 흐려지지 않게 한다.

완료 기준:

- 긴 약관에서도 원문 맥락을 최대한 보존하면서 분석할 수 있다.
- chunking 때문에 약관의 예외나 적용 범위가 빠지는 문제가 줄어든다.

수행 기록:

- 백엔드에 `DIRECT_ANALYSIS_CHAR_LIMIT`, `CHUNK_CHAR_LIMIT`, `MAX_TOTAL_INPUT_CHARS` 기준을 추가했다.
- 50,000자 이하 원문은 기존처럼 한 번에 분석한다.
- 50,000자 초과 원문은 45,000자 단위 chunk로 나누고, chunk별 후보 카드를 먼저 추출한다.
- chunk별 후보 카드를 다시 최종 병합 프롬프트에 넣어 최대 6개 카드로 정리한다.
- chunk별 분석은 카드 0개를 허용하고, 최종 병합 결과만 카드 1개 이상을 요구한다.
- chunk별 1차 분석은 최대 3개까지 병렬 처리해 긴 약관에서 대기 시간을 줄인다.
- 300,000자를 초과하는 원문은 폭주 방지를 위해 413 에러로 거절한다.
- 확장 프로그램 어댑터는 더 이상 `pageText`를 50,000자로 자르지 않고 전체 원문을 전달한다.
- Side Panel 원문 영역은 50,000자까지만 미리보기로 보여주며, 복사와 이후 분석에는 전체 원문을 사용한다.
- `/api/health`에서 chunk 관련 제한값을 확인할 수 있게 했다.

### Phase 7. Side Panel 분석 UI 구현

상태: 완료

- Side Panel 첫 화면에는 서비스 이름 `부비비`, 약관 페이지에서 분석하라는 짧은 사용 안내, `분석하기` 버튼을 제공한다.
- 분석 전, 로딩, 성공, 실패, 빈 텍스트 상태를 만든다.
- 성공 시 요약과 스와이프 가능한 카드 스택을 표시한다.
- 카드별 원문 보기와 확인 체크를 제공한다.
- "웹에서 자세히 보기" 버튼을 제공한다.
- 공간이 좁으므로 카드 UI는 native 스와이프 스택으로 구성하고, 긴 원문은 접기/펼치기로 처리한다.

완료 기준:

- 사용자가 예약 페이지를 떠나지 않고 주의사항을 확인할 수 있다.
- 웹 앱 없이도 핵심 데모가 성립한다.

수행 기록:

- Side Panel 첫 화면에서 출처, 원문, 카드 제목, placeholder를 제거하고 서비스 이름 `부비비`, 사용 안내, `분석하기` 버튼만 남겼다.
- 버튼 한 번으로 현재 페이지 원문 추출과 FastAPI `/api/analyze-reservation` 분석 요청을 연달아 수행하도록 했다.
- 긴 원문은 백엔드 chunking 경로를 사용하며, Side Panel에는 로딩 애니메이션과 순환 작업 단계 메시지를 표시한다.
- 분석 요청은 5분 타임아웃을 두어 무한 대기처럼 보이지 않게 했다.
- 로딩 화면 하단에 긴 분석 소요 시간 안내와 처음부터 다시 시도하는 버튼을 추가했다.
- 분석 전, 추출 중, 분석 중, 성공, 실패 상태를 단일 화면 전환, 로딩 패널, message box로 표시한다.
- 추출/분석 실패 시 첫 화면의 `분석하기` 버튼으로 바로 돌아가지 않고 실패 화면과 `다시 시도` 버튼을 표시한다.
- 분석 성공 시 요약과 native 카드 스택을 Side Panel 안에 렌더링한다.
- 카드는 좌우 버튼과 마우스/터치 스와이프로 넘길 수 있다.
- 카드별 `high`, `medium`, `low` 배지를 표시한다.
- 카드별 쉬운 설명, 확인 질문, 원문 근거 펼침/접힘 UI를 추가했다.
- 카드별 확인 체크와 전체 확인 개수 표시를 추가했다.
- 카드를 스와이프로 넘기면 해당 카드를 자동으로 확인 처리한다.
- `웹에서 보기` 버튼을 추가해 웹 앱 상세 화면을 열 수 있게 했다.
- 원문은 결과 화면 하단의 접힌 드롭다운으로만 제공하고, 긴 경우 50,000자 미리보기만 표시하며 분석과 복사는 전체 텍스트를 사용한다.
- 원문 드롭다운은 카드 그림자와 겹쳐 보이지 않도록 카드 스택 아래 여백을 둔다.

### Phase 8. 웹 앱과 확장 프로그램 연결

상태: 완료

- Side Panel의 분석 결과를 웹 앱 상세 화면으로 넘기는 방식을 정한다.
- 짧은 데모라면 URL query/session 기반 mock 전달을 사용할 수 있다.
- 저장이 필요하면 백엔드에 분석 세션을 만들고 `analysisId`로 웹 앱을 연다.
- 웹 앱은 `analysisId` 또는 전달받은 JSON으로 같은 결과 UI를 보여준다.

완료 기준:

- Side Panel에서 본 결과를 웹 앱에서도 이어서 볼 수 있다.

수행 기록:

- FastAPI에 `POST /api/analysis-sessions`, `GET /api/analysis-sessions/{analysisId}`를 추가했다.
- 분석 세션은 로컬 데모 기준으로 서버 메모리에 최대 50개까지 보관한다.
- Side Panel은 AI 분석 결과를 받은 뒤 세션으로 저장하고 `analysisId`를 보관한다.
- `웹에서 보기` 버튼은 `/result?analysisId=...` URL을 새 탭으로 연다.
- 웹 앱 `App`은 `/result` 경로에서 `Result` 화면을 렌더링한다.
- `Result` 화면은 `analysisId`가 있으면 실제 세션을 불러오고, 없으면 기존 샘플 결과를 보여준다.
- 웹 상세 화면은 Side Panel에서 저장한 분석 카드, 원문, 출처 URL, 페이지 제목을 함께 표시한다.
- 프론트엔드 production build를 다시 생성해 FastAPI 정적 서빙에 반영했다.

### Phase 8-1. 외부 공급업체 약관 이어서 분석

- Trip.com처럼 중개 플랫폼 약관에서 "항공사, 숙소, 투어 공급업체의 별도 약관이 적용된다"는 내용을 감지한다.
- 분석 결과 카드에 "공급업체 약관도 확인 필요" 상태를 표시한다.
- 분석 JSON에 후속 확인이 필요한 이유와 권장 행동을 담을 수 있는 확장 필드를 검토한다.
  - 예시: `needsFollowUpTerms`, `followUpReason`, `suggestedAction`
- Side Panel에서 사용자가 항공사/숙소/공급업체 약관 페이지로 이동한 뒤 다시 분석할 수 있는 흐름을 만든다.
- 이전 분석 결과와 새 약관 분석 결과를 같은 세션으로 묶는 방식을 정한다.
- 웹 앱 상세 화면에서는 "1차 분석: 예약 플랫폼 약관", "2차 분석: 공급업체 약관"처럼 출처별 분석을 구분해서 보여준다.
- 공급업체 약관 링크를 자동으로 찾지 못하는 경우, 사용자가 이동한 현재 페이지를 다시 분석하는 수동 흐름을 우선 제공한다.

완료 기준:

- 중개 플랫폼 약관에서 별도 공급업체 약관 확인 필요성을 안내할 수 있다.
- 사용자는 다른 약관 페이지로 이동한 뒤 같은 Side Panel에서 이어서 분석할 수 있다.
- 여러 약관 분석 결과가 출처별로 구분되어 혼동 없이 표시된다.

### Phase 8-2. 웹 상세 후속 질문 채팅

상태: 완료

- Side Panel이 아니라 `웹에서 보기` 이후 웹 상세 화면에서 채팅을 제공한다.
- 채팅은 `analysisId`로 저장된 분석 카드와 원문을 컨텍스트로 사용한다.
- 사용자는 확인한 위험 조건에 대해 추가 질문을 입력할 수 있다.
- 답변은 원문 근거가 있는 내용 중심으로 작성하고, 확인되지 않는 내용은 확인 어렵다고 안내한다.
- 채팅 답변도 JSON 스키마로 검증하고, 실패 시 복구 요청을 한 번 수행한다.

완료 기준:

- 웹 상세 화면에서 분석 결과 아래에 후속 질문 채팅 UI가 표시된다.
- 사용자의 질문에 대해 원문 근거, 후속 질문, AI 한계 고지를 포함한 답변을 받을 수 있다.

수행 기록:

- FastAPI에 `POST /api/analysis-sessions/{analysisId}/chat`을 추가했다.
- 채팅 요청은 사용자 질문과 최근 대화 이력을 포함한다.
- 채팅 응답은 `answer`, `sourceQuotes`, `followUpQuestions`, `disclaimer` 구조로 반환한다.
- 웹 앱에 `AnalysisChat` 컴포넌트를 추가했다.
- `/result?analysisId=...` 실제 분석 결과 화면에서만 후속 질문 채팅을 표시한다.
- 추천 질문, 답변 스레드, 근거 원문 펼침, 후속 질문 버튼, 입력 폼을 구현했다.

### Phase 8-3. 예약 사이트 진입 안내 팝업

상태: 완료

- 네이버 예약, Trip.com, Airbnb에 들어가면 페이지 안에 작은 `부비비` 안내 팝업을 표시한다.
- 예약 사이트 일반 페이지에서는 `약관 페이지로 이동` 버튼을 눌러 해당 사이트의 공식 약관 페이지로 이동한다.
- 약관 페이지에서는 `부비비 열기` 버튼을 눌러 Chrome Side Panel을 연다.
- `나중에` 또는 닫기 버튼을 누르면 같은 탭 세션에서는 해당 도메인 안내를 다시 띄우지 않는다.
- target adapter가 Airbnb, Trip.com, 네이버 예약 및 네이버 예약 약관 페이지를 구분해 `siteName`, `adapter`, `pageKind`를 반환한다.

완료 기준:

- 대상 예약 사이트 진입 시 사용자가 부비비 분석 기능을 발견할 수 있다.
- 팝업에서 공식 약관 페이지로 이동한 뒤 Side Panel 분석 흐름으로 이어질 수 있다.

### Phase 9. 모두싸인 연동

- AI 요약/주의사항 정리 기능이 완성된 뒤 시작한다.
- 이번 정체성에서 모두싸인의 역할을 재정의한다.
- 가능한 방향:
  - 사용자가 예약 조건 확인 완료 기록에 전자서명
  - 업체와 여행객이 예약 조건 확인서를 함께 보관
  - 분쟁 시 확인 기록을 증빙으로 제공
- Modusign API Key는 서버 환경변수로 관리한다.
- 실패, 인증 오류, rate limit 처리를 포함한다.

완료 기준:

- AI 분석 확인 완료 이후 모두싸인 흐름의 위치와 목적이 제품 안에서 어색하지 않다.

### Phase 10. 데모 마감

- 실제 예약 업체 페이지에서 확장 프로그램을 실행한다.
- Side Panel에서 추출, 분석, 카드 확인이 한 번에 진행된다.
- 웹 앱 상세 보기로 자연스럽게 이동한다.
- API 실패 시 mock fallback으로 발표 흐름이 끊기지 않는다.
- 핵심 문안이 판단/단정이 아니라 이해 보조로 유지된다.

완료 기준:

- 발표자가 2~3분 안에 문제, 사용 장면, AI 분석, 원문 근거, 확인 완료를 보여줄 수 있다.

## 4. 바로 다음 작업 순서

1. 실제 Airbnb 예약/숙소/약관 페이지에서 긴 원문 chunk 분석 결과를 검수한다.
2. Side Panel 분석 결과를 웹 앱 상세 화면으로 전달한다.
3. 공급업체 별도 약관이 필요한 경우 이어서 분석하는 흐름을 붙인다.
4. 모두싸인 연동은 AI 분석 흐름 완성 후 착수한다.

## 5. 리스크와 대응

- 실제 예약 사이트 DOM이 자주 바뀔 수 있다.
  - 대응: 업체 전용 adapter와 generic fallback을 함께 두되, 핵심 약관을 임의로 잘라내지 않는 전체 원문 보존 방식을 우선한다.

- 어댑터가 중요 영역만 선별하면 계약/약관의 맥락을 해칠 수 있다.
  - 대응: adapter는 원문 수집과 민감정보 마스킹에 집중하고, 중요도 판단은 AI 분석 단계로 넘긴다.

- Side Panel 공간이 좁아 카드와 원문이 답답할 수 있다.
  - 대응: 핵심만 먼저 보여주고 원문은 접기/펼치기, 상세는 웹 앱으로 넘긴다.

- AI가 원문에 없는 내용을 만들 수 있다.
  - 대응: `source` 필수, JSON 검증, 판단/단정 금지 프롬프트를 적용한다.

- Trip.com 같은 중개 플랫폼은 실제 취소/환불 조건을 외부 공급업체 약관에 위임할 수 있다.
  - 대응: "별도 약관 확인 필요" 카드를 만들고, 사용자가 해당 약관 페이지에서 Side Panel로 다시 분석할 수 있게 한다.

- API Key가 노출될 수 있다.
  - 대응: 프론트엔드와 확장 프로그램에 키를 두지 않고 백엔드 환경변수로 관리한다.

- 모두싸인의 제품상 역할이 약해질 수 있다.
  - 대응: AI 분석 완료 이후 "예약 조건 확인 기록" 또는 "확인서"로 역할을 재정의한다.
